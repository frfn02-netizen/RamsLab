"use client";

import Link from "next/link";
import { useRouter } from "next/navigation";
import { useEffect, useState, type FormEvent } from "react";
import { useAuth } from "@/components/providers/auth-providers";
import { Badge, Button, Card, ErrorState, Field, LoadingState, inputClass } from "@/components/ui";
import { deleteDosen, getDosenById, updateDosen } from "@/lib/api/modules";
import { getUserFacingError } from "@/lib/api/errors";
import type { Dosen } from "@/types/modules";

export default function DosenDetail({ id }: { id: string }) {
  const { user } = useAuth();
  const router = useRouter();
  const [dosen, setDosen] = useState<Dosen | null>(null);
  const [editing, setEditing] = useState(false);
  const [saving, setSaving] = useState(false);
  const [deleting, setDeleting] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const [form, setForm] = useState({ fullName: "", employeeId: "", title: "", position: "", specialization: "", email: "", phone: "", bio: "", linkedin: "", isPublic: true });

  useEffect(() => { let cancelled = false; getDosenById(id).then((result) => { if (!cancelled) { setDosen(result); setForm({ fullName: result.fullName, employeeId: result.employeeId ?? "", title: result.title ?? "", position: result.position ?? "", specialization: result.specialization.join(", "), email: result.email ?? "", phone: result.phone ?? "", bio: result.bio ?? "", linkedin: result.linkedin ?? "", isPublic: result.isPublic }); } }).catch((reason) => { if (!cancelled) setError(getUserFacingError(reason)); }); return () => { cancelled = true; }; }, [id]);
  const update = (key: string, value: string | boolean) => setForm((current) => ({ ...current, [key]: value }));

  async function save(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setSaving(true); setError(null); try { const result = await updateDosen(id, { fullName: form.fullName, employeeId: form.employeeId || undefined, title: form.title || undefined, position: form.position || undefined, specialization: form.specialization.split(",").map((item) => item.trim()).filter(Boolean), email: form.email || undefined, phone: form.phone || undefined, bio: form.bio || undefined, linkedin: form.linkedin || undefined, isPublic: form.isPublic }); setDosen(result); setEditing(false); } catch (reason) { setError(getUserFacingError(reason)); } finally { setSaving(false); } }
  async function remove() { if (!dosen || !window.confirm(`Delete “${dosen.fullName}”? This removes the lecturer profile.`)) return; setDeleting(true); setError(null); try { await deleteDosen(dosen._id); router.push("/dashboard/dosen"); } catch (reason) { setError(getUserFacingError(reason)); setDeleting(false); } }

  if (!dosen && !error) return <div className="p-5 sm:p-7 lg:p-9"><LoadingState label="Loading dosen" /></div>;
  if (error && !dosen) return <div className="p-5 sm:p-7 lg:p-9"><ErrorState message={error} /><Link href="/dashboard/dosen" className="mt-5 inline-block text-sm font-bold text-[var(--rams-red)]">← Back to dosen</Link></div>;
  if (!dosen) return null;
  return <div className="p-5 sm:p-7 lg:p-9"><div className="mx-auto max-w-4xl space-y-7"><Link href="/dashboard/dosen" className="text-sm font-bold text-[var(--rams-red)]">← All dosen</Link>{error && <ErrorState message={error} />}<div className="flex flex-wrap items-start justify-between gap-4"><div><Badge tone={dosen.isPublic ? "green" : "neutral"}>{dosen.isPublic ? "Public" : "Private"}</Badge><h1 className="mt-4 text-4xl font-bold">{dosen.fullName}</h1><p className="mt-2 text-[var(--rams-gray)]">{dosen.title ?? "Lecturer"}{dosen.position ? ` · ${dosen.position}` : ""}</p></div>{user?.role === "ADMIN" && <div className="flex gap-2"><Button variant="secondary" onClick={() => setEditing((value) => !value)}>{editing ? "Cancel" : "Edit"}</Button><Button variant="danger" disabled={deleting} onClick={() => void remove()}>{deleting ? "Deleting…" : "Delete"}</Button></div>}</div>{editing ? <Card className="p-6"><form onSubmit={save} className="space-y-5"><div className="grid gap-5 sm:grid-cols-2"><Field label="Full name"><input required minLength={2} className={inputClass} value={form.fullName} onChange={(event) => update("fullName", event.target.value)} /></Field><Field label="Employee ID"><input className={inputClass} value={form.employeeId} onChange={(event) => update("employeeId", event.target.value)} /></Field><Field label="Title"><input className={inputClass} value={form.title} onChange={(event) => update("title", event.target.value)} /></Field><Field label="Position"><input className={inputClass} value={form.position} onChange={(event) => update("position", event.target.value)} /></Field><Field label="Email"><input type="email" className={inputClass} value={form.email} onChange={(event) => update("email", event.target.value)} /></Field><Field label="Phone"><input className={inputClass} value={form.phone} onChange={(event) => update("phone", event.target.value)} /></Field></div><Field label="Specializations"><input className={inputClass} value={form.specialization} onChange={(event) => update("specialization", event.target.value)} /></Field><Field label="LinkedIn URL"><input type="url" className={inputClass} value={form.linkedin} onChange={(event) => update("linkedin", event.target.value)} /></Field><Field label="Bio"><textarea className={`${inputClass} min-h-28`} value={form.bio} onChange={(event) => update("bio", event.target.value)} /></Field><label className="flex items-center gap-3 text-sm font-semibold"><input type="checkbox" checked={form.isPublic} onChange={(event) => update("isPublic", event.target.checked)} /> Public profile</label><Button type="submit" disabled={saving}>{saving ? "Saving…" : "Save changes"}</Button></form></Card> : <Card className="grid gap-6 p-6 sm:grid-cols-2"><div><p className="text-xs font-bold uppercase tracking-wide text-[var(--rams-gray)]">Employee ID</p><p className="mt-2">{dosen.employeeId ?? "Not provided"}</p></div><div><p className="text-xs font-bold uppercase tracking-wide text-[var(--rams-gray)]">Email</p><p className="mt-2">{dosen.email ?? "Not provided"}</p></div><div><p className="text-xs font-bold uppercase tracking-wide text-[var(--rams-gray)]">Specializations</p><div className="mt-2 flex flex-wrap gap-2">{dosen.specialization.length ? dosen.specialization.map((item) => <Badge key={item}>{item}</Badge>) : <span>Not provided</span>}</div></div><div><p className="text-xs font-bold uppercase tracking-wide text-[var(--rams-gray)]">Phone</p><p className="mt-2">{dosen.phone ?? "Not provided"}</p></div><div className="sm:col-span-2"><p className="text-xs font-bold uppercase tracking-wide text-[var(--rams-gray)]">Bio</p><p className="mt-2 whitespace-pre-wrap leading-7">{dosen.bio ?? "No biography provided."}</p></div>{dosen.linkedin && <a href={dosen.linkedin} target="_blank" rel="noreferrer" className="font-semibold text-[var(--rams-red)]">LinkedIn profile ↗</a>}</Card>}</div></div>;
}
