"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import { getAlumniList } from "@/lib/api/alumni";
import { getUserFacingError } from "@/lib/api/errors";
import { Badge, Card, EmptyState, ErrorState, LinkButton, LoadingState, PageHeader, Pagination, inputClass } from "@/components/ui";
import { useAuth } from "@/components/providers/auth-providers";
import type { Alumni } from "@/types/alumni";
import { useDebouncedValue } from "@/lib/use-debounced-value";
const limit = 10;
function formatStatus(status: Alumni["currentStatus"]) { return status.replaceAll("_", " ").toLowerCase().replace(/\b\w/g, (letter) => letter.toUpperCase()); }
export default function AlumniPage() { const { user } = useAuth(); const [items, setItems] = useState<Alumni[]>([]); const [total, setTotal] = useState(0); const [page, setPage] = useState(1); const [searchInput, setSearchInput] = useState(""); const search = useDebouncedValue(searchInput.trim(), 300); const [loading, setLoading] = useState(true); const [error, setError] = useState<string | null>(null); useEffect(() => { let cancelled = false; async function loadAlumni() { setLoading(true); setError(null); try { const result = await getAlumniList({ page, limit, search }); if (!cancelled) { setItems(result.data); setTotal(result.total); } } catch (e) { if (!cancelled) setError(getUserFacingError(e)); } finally { if (!cancelled) setLoading(false); } } void loadAlumni(); return () => { cancelled = true; }; }, [page, search]); const totalPages = Math.ceil(total / limit); return <div className="p-5 sm:p-7 lg:p-9"><div className="mx-auto max-w-7xl space-y-7"><PageHeader eyebrow="People" title="Alumni" description="Browse alumni profiles and their current professional journey." action={user?.role === "ADMIN" ? <LinkButton href="/dashboard/alumni/new">Add alumni</LinkButton> : undefined} /><Card className="p-4"><label htmlFor="alumni-search" className="mb-2 block text-sm font-semibold">Search alumni</label><input id="alumni-search" type="search" value={searchInput} onChange={(e) => { setSearchInput(e.target.value); setPage(1); }} placeholder="Name, NIM, or program" className={`${inputClass} max-w-md`} /></Card>{error ? <ErrorState message={error} onRetry={() => setPage(page)} /> : loading && items.length === 0 ? <Card><LoadingState label="Loading alumni" /></Card> : items.length === 0 ? <EmptyState title={search ? "No matching alumni" : "No alumni found"} description={search ? `No alumni matched “${search}”.` : "There are no alumni records available yet."} action={user?.role === "ADMIN" ? <LinkButton href="/dashboard/alumni/new">Create the first profile</LinkButton> : undefined} /> : <Card><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left"><thead className="border-b border-black/8 bg-[var(--rams-gray-light)]"><tr>{["Alumni", "NIM", "Program", "Graduation", "Status", "Company", "Action"].map((heading) => <th key={heading} scope="col" className="px-5 py-4 text-xs font-bold uppercase tracking-wide text-[var(--rams-gray)]">{heading}</th>)}</tr></thead><tbody className="divide-y divide-black/8">{items.map((item) => <tr key={item._id} className={loading ? "opacity-60" : ""}><td className="px-5 py-4"><p className="font-semibold">{item.fullName}</p><p className="mt-1 text-xs text-[var(--rams-gray)]">{item.currentPosition ?? "Position not provided"}</p></td><td className="px-5 py-4 text-sm">{item.nim}</td><td className="px-5 py-4 text-sm">{item.program}</td><td className="px-5 py-4 text-sm">{item.graduationYear}</td><td className="px-5 py-4"><Badge tone={item.currentStatus === "SEEKING_JOB" ? "amber" : "neutral"}>{formatStatus(item.currentStatus)}</Badge></td><td className="px-5 py-4 text-sm">{item.currentCompany ?? "—"}</td><td className="px-5 py-4 text-right"><Link href={`/dashboard/alumni/${item._id}`} className="text-sm font-bold text-[var(--rams-red)] hover:text-[var(--rams-red-dark)]">View</Link></td></tr>)}</tbody></table></div><Pagination page={page} totalPages={totalPages} disabled={loading} onPageChange={setPage} /></Card>}</div></div>; }
