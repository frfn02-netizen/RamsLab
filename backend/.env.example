PORT=5000

MONGODB_URI=mongodb://127.0.0.1:27017
DB_NAME=rams_platform

JWT_SECRET=replace-with-a-random-secret-at-least-32-characters

JWT_EXPIRES_IN=1d

FRONTEND_URL=http://localhost:3000
PUBLIC_API_URL=http://localhost:5000
NODE_ENV=development

SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_PASSWORD=replace-with-a-unique-12-character-password
SEED_DOSEN_EMAIL=dosen@example.com
SEED_DOSEN_PASSWORD=replace-with-a-unique-12-character-password

# Development-only contributor seed; the seed command refuses NODE_ENV=production.
# SEED_PUBLICATION_EDITOR_EMAIL=publication-editor@example.test
# SEED_PUBLICATION_EDITOR_PASSWORD=replace-with-a-unique-12-character-password
